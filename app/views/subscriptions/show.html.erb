<div class="plans banner_principal uk-width-1-1"  style="background-size: cover; background-repeat: no-repeat;  min-height : 100%">
	<div class="uk-vertical-align uk-text-center uk-height-2-2">
		<div class="uk-vertical-align-middle" style="width: 500px; margin: 10%">
			<% if !@has_active_subscription %>
				<div class="uk-panel uk-panel-box uk-text-center">
						<h3>Olá <%= @user.name || @user.email %></h3>
						<p>Clique em confirmar para contratar a assinatura abaixo.</p>
						<p>Você receberá um email com os detalhes de pagamento.</p>
						<table class="uk-table">
							<thead>
								<tr>
									<td>Item</td>
									<td>Valor</td>
								</tr>
							</thead>
							<tbody>
								<tr>
									<td>Plano <%= @plan.name %></td>
									<td>R<%= number_to_currency(@plan.price) %></td>
								</tr>
								<tr>
									<td class="uk-text-right">TOTAL:</td>
									<td>R<%= number_to_currency(@plan.price) %></td>
								</tr>
							</tbody>
						</table>
						<%= form_for @subscription do |f| %>
							<%=  hidden_field(:subscription, :plan_id) %>
							<input type="submit" value="CONFIRMAR COMPRA" />
						<% end %>
				</div>
			<% else %>
				<div class="uk-panel uk-panel-box uk-text-center">
					<h3>Você já possui uma assinatura</h3>
					<a href="/dashboard">Clique aqui para acessar seu dashboard</a>
				</div>
			<% end %>
		</div>
	</div>
</div>